instance_activate_layer("options");


var lay_id = layer_get_id("main_menu");
if instance_deactivate_layer(lay_id)
   {
   instance_deactivate_layer(lay_id);
   }
else
   {
   instance_deactivate_layer(lay_id);
   }
   